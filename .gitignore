# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Env — indeholder hemmeligheder, må aldrig committes
.env
.env.local
.env.*.local

# Netlify
.netlify

# Supabase CLI lokal state
supabase/.temp

# Claude Code harness / lokal config — behold kun launch.json (dev-server-config)
.claude/*
!.claude/launch.json

# Lokale APV-dokumenter/PDF'er (kopieret ind til Storage-upload — hører ikke i git)
Gamle apv filer/

# TS build info
*.tsbuildinfo
